apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

# No secrets needed for Portainer
# No PVC (using emptyDir for simplicity - change for production)